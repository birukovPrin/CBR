{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

{{ form_start(form) }}
    {{ form_errors(form) }}
{{ form_end(form) }}

{% if rates is not empty %}
    Курс {{ rates|last }}
    Предыдущий день {{ rates|first }}
    <br>
    Разница с предыдущим днем {{ (((rates|last)-(rates|first))/(rates|first))*100 }} %
{% endif %}
<br>
{{message}}

{% endblock %}